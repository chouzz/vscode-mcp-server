{
  "name": "vscode-lsp-mcp",
  "displayName": "vscode-lsp-mcp",
  "description": "Expose model-friendly symbol navigation tools from VS Code over MCP.",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.103.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "vscode-lsp-mcp.showServerInfo",
        "title": "VSCode LSP MCP: Show Server Info"
      },
      {
        "command": "vscode-lsp-mcp.copyServerUrl",
        "title": "VSCode LSP MCP: Copy MCP URL"
      },
      {
        "command": "vscode-lsp-mcp.showLogs",
        "title": "VSCode LSP MCP: Show Logs"
      }
    ],
    "configuration": {
      "title": "VSCode LSP MCP",
      "properties": {
        "vscode-lsp-mcp.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable the local MCP server."
        },
        "vscode-lsp-mcp.host": {
          "type": "string",
          "default": "127.0.0.1",
          "description": "Host address used by the local MCP server."
        },
        "vscode-lsp-mcp.basePort": {
          "type": "number",
          "default": 9527,
          "minimum": 1,
          "maximum": 65535,
          "description": "Base port used to derive a stable workspace-specific MCP port."
        },
        "vscode-lsp-mcp.portRangeSize": {
          "type": "number",
          "default": 200,
          "minimum": 1,
          "maximum": 2000,
          "description": "How many ports are reserved for stable workspace-specific allocation."
        },
        "vscode-lsp-mcp.cors.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable CORS headers for browser-based MCP clients."
        },
        "vscode-lsp-mcp.cors.allowOrigins": {
          "type": "string",
          "default": "*",
          "description": "Allowed origins for CORS. Use '*' or a comma-separated list."
        },
        "vscode-lsp-mcp.cors.withCredentials": {
          "type": "boolean",
          "default": false,
          "description": "Whether CORS requests may include credentials."
        },
        "vscode-lsp-mcp.logLevel": {
          "type": "string",
          "enum": [
            "debug",
            "info",
            "warn",
            "error"
          ],
          "default": "info",
          "description": "Minimum log level written to the VSCode LSP MCP output channel."
        },
        "vscode-lsp-mcp.showStartupNotification": {
          "type": "boolean",
          "default": true,
          "description": "Show a notification when the server starts or reuses an existing workspace server."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run esbuild-base -- --minify",
    "esbuild-base": "esbuild ./src/extension.ts --bundle --outfile=out/main.js --external:vscode --format=cjs --platform=node",
    "compile": "webpack",
    "watch": "webpack --watch",
    "package": "pnpm vsce package --no-dependencies",
    "publish": "pnpm vsce publish --no-dependencies",
    "vsix": "vsce package --no-dependencies",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "pnpm run compile-tests && pnpm run compile && pnpm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.103.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "@vscode/vsce": "^3.7.1",
    "esbuild": "^0.27.4",
    "eslint": "^9.39.3",
    "ts-loader": "^9.5.4",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.56.1",
    "webpack": "^5.105.3",
    "webpack-cli": "^6.0.1"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "express": "^5.2.1",
    "zod": "^4.3.6"
  }
}
